# Login Client/Server Application

Prerequisites:

- Node 8.11.1+

## Starting the Login Server
1. *Run the server*
```
cd server
npm install
npm start
```
## Starting the Login Client
 1. *Run the client*
 ```
 cd client
 npm install
 npm start
 ```